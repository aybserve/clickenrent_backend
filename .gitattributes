# Использовать LF (Unix style) в Git, но CRLF в рабочей копии на Windows
* text=auto

# Шелл файлы всегда LF
*.sh text eol=lf

# Java, XML, YAML — LF (рекомендуется для Spring, Docker, K8s)
*.java text eol=lf
*.xml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.properties text eol=lf

# Windows-скрипты — CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Bash scripts, Docker, Jenkins — только LF
Dockerfile text eol=lf
*.dockerfile text eol=lf
Jenkinsfile text eol=lf