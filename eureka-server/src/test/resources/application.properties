# Use random port in tests to avoid conflicts (e.g. in CI)
server.port=0

# Eureka client already disabled in main config; ensure no registration in tests
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
