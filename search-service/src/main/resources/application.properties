# Application Info
spring.application.name=search-service
info.author=Vitaliy Shvetsov

#PROFILE CONFIGURATION
# Spring Boot automatically reads SPRING_PROFILES_ACTIVE from environment
# DotenvConfig loads .env file, which contains SPRING_PROFILES_ACTIVE=staging

server.port=8086

# Elasticsearch Configuration
spring.elasticsearch.uris=${ES_URIS:http://localhost:9200}
spring.elasticsearch.username=${ES_USERNAME:elastic}
spring.elasticsearch.password=${ES_PASSWORD}
spring.elasticsearch.connection-timeout=5s
spring.elasticsearch.socket-timeout=10s

# Enable automatic index creation when documents are saved
spring.data.elasticsearch.repositories.enabled=true

# Eureka Service Discovery
#eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}
#eureka.client.register-with-eureka=true
#eureka.client.fetch-registry=true
#eureka.instance.prefer-ip-address=true
#eureka.instance.instance-id=${spring.application.name}:${random.value}
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true

# Eureka health check configuration
eureka.client.healthcheck.enabled=true
eureka.instance.health-check-url-path=/actuator/health
eureka.instance.status-page-url-path=/actuator/info
eureka.instance.lease-renewal-interval-in-seconds=30
eureka.instance.lease-expiration-duration-in-seconds=90

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Feign Client Configuration
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=10000
feign.client.config.default.loggerLevel=full

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.health.eureka.enabled=false

# Indexing Configuration
search.indexing.batch-size=${INDEXING_BATCH_SIZE:500}
search.indexing.async-pool-size=${INDEXING_ASYNC_POOL_SIZE:5}

# Scheduled Sync Configuration
search.scheduled-sync.enabled=${SCHEDULED_SYNC_ENABLED:true}
search.scheduled-sync.cron=${SCHEDULED_SYNC_CRON:0 0 2 * * *}

# Logging
logging.level.root=INFO
logging.level.org.clickenrent.searchservice=DEBUG
logging.level.org.springframework.cloud.openfeign=DEBUG
logging.level.org.elasticsearch=DEBUG

# Sentry Configuration
# Leave empty to disable Sentry, or set SENTRY_DSN_SEARCH in .env file
sentry.dsn=${SENTRY_DSN_SEARCH:}
sentry.environment=${SENTRY_ENVIRONMENT:production}
sentry.release=1.0-SNAPSHOT@search-service
sentry.traces-sample-rate=${SENTRY_TRACES_SAMPLE_RATE:1.0}
sentry.send-default-pii=false
sentry.enable-tracing=true
sentry.logging.minimum-event-level=error
sentry.logging.minimum-breadcrumb-level=info
sentry.tags.service=search-service
