# =====================================================================================================================
# NOTIFICATION SERVICE - STAGING PROFILE OVERRIDES
# =====================================================================================================================
# Activate with: spring.profiles.active=staging
#
# In staging, Hibernate creates/updates tables (ddl-auto=update) and Flyway
# loads sample/test data from db/testdata/ in addition to db/migration/.
# =====================================================================================================================

# Hibernate creates/updates tables in staging
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
